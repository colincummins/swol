# Port the API will accept connections on
PORT=3000

# PostgreSQL connection (pg reads PG* vars automatically)
PGHOST=localhost
PGPORT=5432
PGDATABASE=swol
PGUSER=
PGPASSWORD=

# Allowed CORS origin for the UI (use * to allow all during local dev)
CORS_ORIGIN=http://localhost:3001
